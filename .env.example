MONO_SECRET_KEY=
MONO_WEBHOOK_SECRET=
FIREBASE_PROJECT_ID=tradr-483b9
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
PORT=3000

# Africa's Talking SMS — https://africastalking.com
# Sign up → get API key immediately. Use username "sandbox" + sandbox API key for testing.
# Sandbox sends to the AT simulator (free, no real SMS). Switch to your real username for production.
AT_API_KEY=
AT_USERNAME=sandbox
# Optional — sender ID registered with AT (leave blank to use a shared shortcode)
AT_SENDER_ID=TRADR
